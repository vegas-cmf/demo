{#  see graydon video for responsive version and iframe api #}
<div class="youtube-container">
    <iframe 
        width="{{width}}" 
        height="{{height}}" 
        src="//www.youtube-nocookie.com/embed/{{code}}?rel=0" 
        frameborder="0" 
        allowfullscreen
    ></iframe>
</div>